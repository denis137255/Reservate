import { AppRegistry } from 'react-native';
import LoginScreen from './screens/LoginScreen'; // Update the path

const Reservate = () => {
  return <LoginScreen />;
};

AppRegistry.registerComponent('Reservate', () => Reservate);
